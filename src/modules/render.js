/**Render Module
 * is called by onclick listeners via MenuBar.renderPage method
 * dynamically renders the page selected in the .main section
 */

class RenderPage {

    static render(page) {
        // TODO 
        // Use if statements based on which category/section passed in as argument
    }
}